#include<stdio.h>

int main()
{
    int no = 1 ;
    if (no % 2 != 0)
    printf("%d\n",no);

    no++;
    if (no % 2 != 0)
    printf("%d\n",no);

    no++;
    if (no % 2 != 0)
    printf("%d",no);

    no++;
    if (no % 2 != 0)
    printf("%d\n",no);

}